# When set, the app calls the real core-infra webhook instead of using
# the mock fixtures in src/lib/mockData.ts. See docs/CONTRACTS.md for
# the expected endpoints and payload shapes.
# VITE_WEBHOOK_BASE_URL=https://core-infra.internal/api/v1
